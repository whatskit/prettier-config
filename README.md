# `prettier-config-whatskit`


## Usage

**Install**:

```bash
$ yarn add --dev prettier-config-whatskit
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "prettier-config-whatskit"
}
```
